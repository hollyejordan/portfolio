import styles from "./ProjectCard.module.css"

type ProjectCardProps = {
}

export default function ExploreCard({ }: ProjectCardProps) {

  return (
    <div>
        <p>project card</p>
    </div>
  );
}